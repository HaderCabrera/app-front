/** @type {import('next').NextConfig} */
const nextConfig = {

    i18n: {
        locales: ["en", "pt"], // Idiomas soportados: inglés (en) y portugués (pt)
        defaultLocale: "en",   // Idioma por defecto
      },
};

export default nextConfig;
